# VA 银行地址排版设计

## 目标

“使用专属 VA 收款”弹窗中，左侧“银行地址”标签保持单行；右侧银行地址占用剩余宽度，内容过长时自然换行。

## 设计

复用现有 `Detail` 行组件，不改变弹窗宽度、字段顺序或数据。标签禁止换行并禁止收缩；值区域允许收缩，使用安全的长文本换行规则。该布局规则适用于弹窗内所有详情行，避免其他标签和长值出现相同问题。

## 验证

先添加失败的源码契约测试，再做最小样式修改；运行专项测试、TypeScript、i18n、变更 lint 和 Git whitespace 检查。
